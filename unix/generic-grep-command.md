### Generic Grep Command

When searching for something across many directories recursively, I always would
just use `grep -r` but an improvement on it is `grep -rni` which enables:

- Recursive
- Case insensitive
- Prints the line number

Source: Anuj
